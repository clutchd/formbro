{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../convex && bunx convex deploy --cmd 'cd ../.. && bunx turbo run build --filter=@formbro/web' --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL && if [ \"$VERCEL_ENV\" = \"production\" ]; then bunx convex run system/initialize:init --prod; else bunx convex run system/initialize:init --preview-name \"$VERCEL_GIT_COMMIT_REF\"; fi"
}
